extends layout

block content
  table#workList
    tr
      th Start
      th Duration
      th 
    each work in works
      tr
        td= work.start
        td= work.duration
        td
          a(href='/work/' + work.id) E
        td
          a(href='/delete/' + work.id) X
